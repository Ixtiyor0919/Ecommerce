import { LoaderWrapper, LoaderContent } from "./Loader.styled";

const Loader = () => {
    return(
        <LoaderWrapper>
            <LoaderContent />
        </LoaderWrapper>
    )
};
export default Loader;